# DB 설정
# SQLite (기본값 - 값을 생략하거나 아래처럼 설정)
DATABASE_URL=sqlite:///test.db

# MySQL 전환 시 아래 형식으로 교체
# DATABASE_URL=mysql://user:password@localhost:3306/wone_db

# 세션 키 (운영 배포 시 반드시 변경)
SESSION_SECRET_KEY=wone-sample-dev-key-change-in-production
